const owuBaseURL = 'http://owu.linkpc.net/carsAPI/v1'

const jsonBaseURL = 'https://jsonplaceholder.typicode.com/'

const urls = {
    owu: {
        cars:'/cars'
    },
    json: {
        users: '/users',
        comments: '/comments'
    }
}

export {
    owuBaseURL,
    jsonBaseURL,
    urls
}